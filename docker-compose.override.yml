services:
  netbox:
    image: ${NETBOX_IMAGE-netbox:latest-plugins}
    pull_policy: never
    ports:
      - 8000:8080
    build:
      context: .
      dockerfile: Dockerfile-Plugins
  netbox-worker:
    image: ${NETBOX_IMAGE-netbox:latest-plugins}
    pull_policy: never
